{{- if .Values.enabled }}


---
apiVersion: v1
kind: ConfigMap 
metadata:
  name: kairosdb-config
  labels:
{{ include "kairosdb.labels" . | indent 4 }}
data:
  overrides.conf: {{ template "merge.keys" (dict "root" . "configKey" "config") }}

{{- end }}
